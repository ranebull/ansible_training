<html>
  <head>
   <title>Welcome to ansible</title>
  </head>
  <body>
  <h1>nginx configured by Ansible</h1>

  <p>If you can see this, Ansible successfully installed nginx.</p>

  <p>{{ template_host }}</p>
  <p>{{ template_uid }}</p>
  <p>{{ template_run_date }}</p>
  <p>{{ template_path }}</p>
  <p>{{ template_fullpath }}</p>
  <p>{{ ansible_managed }}</p>
  </body>
</html>